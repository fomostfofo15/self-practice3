# 📚 Progression — สิ่งที่ได้เรียนรู้ในอาทิตย์นี้

โจทย์แบบฝึกหัดในอาทิตย์นี้ได้มาจากเรียนรู้เรื่องการเขียน timezone โดยใช้ภาษา JS 
นำ Date/month/year ในการมาคำนวณ 
โดยมีการประกาศตัวแปรในการรับมา 2 ค่า คือ birthday และ today
const birthday = new Date(input);
const today = new Date();
และ มีการใช้Logic ที่ไม่ได้มีความซับซ้อนและสามารถเรียนรู้ในการนำมาปรับใช้ได้จริง
let years = today.getFullYear() - birthday.getFullYear();
let months = today.getMonth() - birthday.getMonth();
let days = today.getDate() - birthday.getDate();
รวมถึงการเขียนเงื่อนไข เมื่อค่าที่ได้เป็นวันที่ ติดลบ (เนื่องจาก ยังไม่ถึงวันเกิดของตนเองเพราะวิธีการคำนวณ คือการนำ วันที่ปัจจุบัน - วันเกิด เช่น หากวันนี้คือวันที่ 5 มกรา แต่ค่า birthday ที่รับมาเป็น 20 มกรา 5 - 20 = - 15 จึงต้องมีการเขียนเงื่อนไขในการ เขียน month-- และ days += lastMonth.getDate();)